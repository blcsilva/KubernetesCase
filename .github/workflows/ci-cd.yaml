name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # Atualizando para v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2  # Atualizando para v2
        with:
          terraform_version: 1.0.0

      - name: Initialize Terraform
        run: terraform init
        working-directory: terraform

      - name: Apply Terraform
        run: terraform apply -auto-approve
        working-directory: terraform
        env:
          TF_VAR_k8s_host: ${{ secrets.K8S_HOST }}
          TF_VAR_k8s_token: ${{ secrets.K8S_TOKEN }}
          TF_VAR_k8s_ca_certificate: ${{ secrets.K8S_CA_CERTIFICATE }}

  deploy:
    runs-on: ubuntu-latest
    needs: terraform
    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # Atualizando para v3

      - name: Set up kubectl
        run: |
          echo "${{ secrets.K8S_CA_CERTIFICATE }}" | base64 --decode > ca.crt
          echo "${{ secrets.K8S_TOKEN }}" > token
          kubectl config set-cluster my-cluster --server=${{ secrets.K8S_HOST }} --certificate-authority=ca.crt
          kubectl config set-credentials m
